<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sweetcake">

        <!-- 1. PANTALLA DE INICIO (LAUNCHER) -->
        <activity
            android:name=".activitys.splashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 2. OTRAS PANTALLAS DE LA APP (AQUÍ ESTÁ LA CORRECCIÓN) -->
        <activity
            android:name=".activitys.Bienvenida"
            android:exported="false" />

        <!--  AÑADIDO: Declaración de la pantalla de Login -->
        <activity
            android:name=".activitys.loginactivity"
            android:exported="false" />

        <activity
            android:name=".activitys.activity_registro"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true" />

    </application>

</manifest>
